version: '3.8'
services:
  simulyn:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
